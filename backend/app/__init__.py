"""Top-level package for the backend app.

Creating this file ensures `app` is a regular Python package and
avoids import issues in environments that don't support implicit
namespace packages (or to be explicit for tooling).
"""
